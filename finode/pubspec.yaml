name: finode
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  finode_platform_interface:
    path: ../finode_platform_interface

  finode_linux:
    path: ../finode_linux

  finode_macos:
    path: ../finode_macos

  finode_windows:
    path: ../finode_windows

  finode_android:
    path: ../finode_android

  finode_ios:
    path: ../finode_ios

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

